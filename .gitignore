# ============ Laravel ============
/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# ============ Python ============
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environments
venv/
.venv/
env/

# ============ ML artifacts ============
# Model weights, ~1.3 GB per folder - too large for GitHub. Use the HF Hub or
# Git LFS to share them.
ml/models/
*.safetensors
*.bin
*.pt
*.pth
*.ckpt

# Training data and prediction inputs (large - keep out of the repo).
# Both spellings: ml/dataset/ is the current single folder, ml/datasets/ is the
# named-dataset layout the OCR workspace moves to.
ml/dataset/
ml/datasets/
ml/new_images/

# ============ OS / editor ============
.DS_Store
Thumbs.db

# ============ Legacy prototype ============
# Web prototype - generated saved documents
web/saved_docs/

#kiro IDE
/.kiro